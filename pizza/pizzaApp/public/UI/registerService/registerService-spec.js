describe('registerService', function() {

  beforeEach(module('pizzaApp'));

  it('should ...', inject(function(registerService) {

	//expect(registerService.doSomething()).toEqual('something');

  }));

});